{
  "name": "gameindexu",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "electron.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:exe": "npm run build && npx electron-packager . GameIndexu --platform=win32 --arch=x64 --out=release-build --overwrite"
  },
  "devDependencies": {
    "electron": "^41.2.0",
    "electron-packager": "^17.1.2",
    "vite": "^8.0.1"
  }
}